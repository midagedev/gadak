// Package teamconfig builds and applies team-shareable scry configuration files
// (views, field maps, group rules, …) without credentials or personal machine
// prefs. Export is whitelist-only so new Config fields never leak by default.
package teamconfig
