# Sourced as $ENV by the recording's /bin/sh (see terminal.config.ts).
# One job: a bare prompt. macOS /bin/sh is bash in sh mode and stamps its
# own `sh-3.2$` over any PS1 inherited from the environment, which puts a
# bash version number in the frame.
PS1='$ '
