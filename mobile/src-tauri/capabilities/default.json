{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core permissions only. No plugin permissions until a native capability is actually adopted (Keychain/QR/notifications; see the round report gap map).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
